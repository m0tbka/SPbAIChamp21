from model.building import Building
from model.building_type import BuildingType


"""Идея: есть метод, который принимает тип строения(BuildingType) и его количество.
            Он же должен возвращать список зданий(Building) отсортированных по приоритету(что лучше раньше строить)"""


def calculate(building_to_build: BuildingType, amount_of_building: int):
    pass
